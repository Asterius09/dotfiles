#
# ~/.bashrc
#

# If not running interactively, don't do anything
# [[ $- != *i* ]] && return

# alias ls='ls --color=auto'
# PS1='[\u@\h \W]\$ '
# . "$HOME/.cargo/env"
# export EDITOR=nano

# Custom aliases set for myself
alias lol="sudo sh -c 'sysctl -w abi.vsyscall32=0'" #> Alias for League of Legends Anti-cheat bypass.
